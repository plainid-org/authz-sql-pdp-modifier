{{- if .Values.secretFiles }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "authz-bigquery.fullname" . }}
  labels:
{{- include "authz-bigquery.labels" . | nindent 4 }}
stringData:
{{- range $path, $config := .Values.secretFiles }}
  {{ $path }}: |
{{ $config | indent 4 -}}
{{- end -}}
{{- end -}}
