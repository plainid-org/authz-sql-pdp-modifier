apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authz-bigquery.fullname" . }}-test-connection"
  labels:
    {{- include "authz-bigquery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authz-bigquery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
