apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authz-sql-pdp-modifier.fullname" . }}-test-connection"
  labels:
    {{- include "authz-sql-pdp-modifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authz-sql-pdp-modifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
